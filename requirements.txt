gradio>=5.0.0,<6.0.0          # main web UI library (4.x is stable in early 2025)
requests>=2.28.0              # HTTP requests
pyyaml>=6.0                   # YAML parsing (used via import yaml)
typing-extensions>=4.0.0      # better typing support (especially if using older Python)

# Optional but commonly useful in Gradio apps:
pillow>=9.0.0                 # image handling (very often needed)
numpy>=1.23.0                 # numeric arrays (frequently used together with Gradio)
pandas>=2.0.0                 # data frames (very common in data apps)
streamlit